# The URL the site will be built for
base_url = "http://pksunkara.com"

title = "pksunkara"
description = "My personal blog"

# Whether to do syntax highlighting
# Theme can be customised by setting the `highlight_theme` variable to a theme supported by Zola
highlight_code = true
highlight_theme = "demain"

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = false

# Whether to generate a RSS feed automatically
generate_rss = true

# TODO: RSS for each section
taxonomies = [
	{ name = "tags", paginate_by = 10, rss = true },
	{ name = "categories", paginate_by = 10, rss = true },
	{ name = "series", paginate_by = 10, rss = true }, # TODO: Taxonomy weight
]

[extra]
# Put all your custom variables here
twitter = "pksunkara"
github = "pksunkara"
facebook = "pksunkara"

disqus = "pksunkara"
google_analytics = "UA-9209860-4"
